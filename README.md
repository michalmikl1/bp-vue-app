# Vue Task Manager Migration

This project is a Vue 3 + Vite migration of the read-only React reference in `../bp-react-app`.

Scripts:

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`
